# 🐱 Environment Variables สำหรับฟูกุแมวขาว

# LINE Bot Configuration
# ดึงค่าเหล่านี้จาก LINE Developers Console
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
LINE_CHANNEL_SECRET=your_channel_secret_here

# Database Configuration
# สำหรับ Supabase PostgreSQL
DATABASE_URL=postgresql://username:password@host:port/database

# Authentication (สำหรับอนาคต)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Vercel Configuration (ตั้งค่าใน Vercel Dashboard)
# VERCEL_URL จะถูกตั้งค่าอัตโนมัติโดย Vercel

# Migration Security
# สำหรับรันการ migrate ฐานข้อมูล (ใช้ secret key ที่ปลอดภัย)
MIGRATION_SECRET=your_super_secret_migration_key_here

# Development Access
# สำหรับการใช้งาน dev tools และ demo data (dev only)
DEV_ACCESS_KEY=your_dev_access_key_here
