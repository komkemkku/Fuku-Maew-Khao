{
  "buildCommand": "next build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/webhook/route.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["sin1"],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
